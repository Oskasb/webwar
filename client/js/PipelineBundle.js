define([
        'PipelineAPI'
    ],
    function(
        PipelineAPI
    ) {
        

        var PipelineBundle = function(srcUrl, onLoaded) {

        };
        
        
        return PipelineBundle
    });