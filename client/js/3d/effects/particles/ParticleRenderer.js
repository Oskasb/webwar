"use strict";

define([
        '3d/effects/particles/SPEGroup',
        '3d/effects/particles/ParticleMaterial',
        'PipelineObject'
    ],
    function(
        SPEGroup,
        ParticleMaterial,
        PipelineObject
    ) {


        
        

        var ParticleRenderer = function() {
            

        };

        ParticleRenderer.prototype.initParticleRenderer = function(effectData, pos, vel) {


        };

        ParticleRenderer.prototype.spawnParticleEffect = function(effectData, pos, vel) {


        };

        return ParticleRenderer;

    });